- name: Create directory
  hosts: all
  gather_facts: true
  become: false
  tasks:
    - name: Create directory in home
      file:
        path: "~/my_directory"
        state: directory
        mode: "0755"
